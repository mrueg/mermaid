import { AbstractMermaidTokenBuilder } from '../common/index.js';

export class TeamTopologyTokenBuilder extends AbstractMermaidTokenBuilder {
  public constructor() {
    super(['teamTopology']);
  }
}
